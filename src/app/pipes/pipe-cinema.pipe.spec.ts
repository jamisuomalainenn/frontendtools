import { PipeCinemaPipe } from './pipe-cinema.pipe';

describe('PipeCinemaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCinemaPipe();
    expect(pipe).toBeTruthy();
  });
});
